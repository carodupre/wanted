<div class="container">
  <div class="row justify-content-center">
    <div class="col-md-8">

  <h1>Edit your booking's address</h1>

  <%= simple_form_for (@booking) do |f| %>
  <%= f.input :address %>
  <%= f.button :submit, class: 'btn btn-primary' %>
  <% end %>

  <p><%= link_to 'Cancel edit mode', booking_path(params[:id]) %></p>

    </div>
  </div>
</div>
